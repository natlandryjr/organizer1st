/**
 * Default organization ID created by the multi-tenancy migration.
 * Used when no auth/session provides an organization context.
 * Replace with actual org from session when auth is implemented.
 */
export const DEFAULT_ORGANIZATION_ID = "cldefaultorg000000000000000";
