# Database - PostgreSQL (required)
# Local: postgresql://user:pass@localhost:5432/organizer1st
# Fly Postgres: set automatically when you run "fly postgres attach"
DATABASE_URL="postgresql://user:password@localhost:5432/organizer1st"

# Stripe (https://dashboard.stripe.com/apikeys)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_...
STRIPE_SECRET_KEY=sk_test_...

# App URL for Stripe redirects
# Local: http://localhost:3000
# Fly: https://your-app.fly.dev
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Auth - JWT secret (generate a random string for production)
JWT_SECRET=your-secret-key-change-in-production

# Email - SMTP (for sending attendee emails)
# SendGrid: smtp.sendgrid.net, port 587, user apikey, pass=your SendGrid API key
# Gmail: smtp.gmail.com, port 587, use App Password
SMTP_HOST=smtp.sendgrid.net
SMTP_PORT=587
SMTP_USER=apikey
SMTP_PASS=your-sendgrid-api-key
EMAIL_FROM=noreply@yourdomain.com
